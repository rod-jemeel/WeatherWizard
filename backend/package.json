{
  "name": "weather-app-backend",
  "version": "1.0.0",
  "description": "Backend API for the Weather App",
  "main": "main.py",
  "scripts": {
    "start": "python main.py",
    "dev": "python -m flask --app main run --host=0.0.0.0 --port=5000 --debug",
    "test": "python -m pytest",
    "test:coverage": "python -m pytest --cov=. --cov-report=term-missing"
  },
  "engines": {
    "node": ">=18",
    "python": ">=3.9"
  },
  "author": "",
  "license": "MIT"
}